name: Deploy to Azure App Service

on:
  push:
    branches:
      - azure-migration  # Deploy when pushing to azure-migration branch
  workflow_dispatch:  # Allow manual triggering

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r app/requirements.txt

    - name: Run tests
      run: |
        python -m pytest app/tests/ -v --tb=short

    - name: Create deployment package
      run: |
        zip -r deploy.zip \
          app/ \
          scripts/ \
          startup.sh \
          requirements.txt \
          -x "*.pyc" \
          -x "**/__pycache__/*" \
          -x "**/tests/*" \
          -x "**/.env*" \
          -x "**/test_*.py"

    - name: Azure Login
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Clear old deployment files
      run: |
        az webapp config appsettings set \
          --resource-group rg-arec-crm \
          --name arec-crm-app \
          --settings SCM_DO_BUILD_DURING_DEPLOYMENT=false \
          --output none

    - name: Deploy to Azure Web App (clean)
      run: |
        az webapp deploy \
          --resource-group rg-arec-crm \
          --name arec-crm-app \
          --src-path deploy.zip \
          --type zip \
          --clean true

    - name: Restart App Service
      run: |
        az webapp restart \
          --resource-group rg-arec-crm \
          --name arec-crm-app

    - name: Check deployment status
      run: |
        echo "Deployment complete. Check app at:"
        echo "https://arec-crm-app.azurewebsites.net/crm"
