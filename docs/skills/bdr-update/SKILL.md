---
name: bdr-update
description: "Placeholder — skill not yet created."
---

# bdr-update

This skill has not been created yet. Create it via skill-creator in Cowork, then sync the live copy here:

```bash
cp ~/.skills/skills/bdr-update/SKILL.md \
   ~/Dropbox/projects/arec-crm/docs/skills/bdr-update/SKILL.md
cd ~/Dropbox/projects/arec-crm && git add docs/skills/bdr-update/ && git commit -m "skills: add bdr-update"
```
